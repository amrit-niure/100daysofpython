s1={1,2,4,6,7}
s2={1,3,5,6,9}
print(s1.union(s2))
s1.update(s2)
print(s1.intersection(s2))
s1.intersection_update(s2)
# symmetric difference ------------- what is not common in two sets
#difference 
#remove(5)
#pop() it pops out random value 
# del s1 it will delete the whole set
# s1.clear() it will clear the elements form the set not set itself
#cities.isdisjoint(cities2)
#issuperset(cities2)
print(s1,s2)

